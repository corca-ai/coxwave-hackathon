코딩을 시작하기 전에, 문제가 복잡하거나 명확하지 않다면 아래 항목을 포함한 Problem 1-Pager를 작성한 뒤 진행해. 불분명한 항목이 있다면 인터뷰를 요청해서 내용을 명확히 하도록 해.

- 배경(Background): 변경이 필요한 맥락과 동기
- 문제(Problem): 우리가 해결하려는 이슈는 무엇인가?
- 목표(Goal): 성공의 기준(성공한 상태)은 무엇인가?
- 비목표(Non-goals): 명확히 범위 밖(스코프 아웃)인 것은 무엇인가?
- 제약(Constraints): 반드시 준수해야 할 기술적/비즈니스적 제약사항

## 협업 가이드 (이번 세션에서 합의된 스타일)
- **관측 가능성 기본값**: 데모/테스트는 기본적으로 입력·출력 payload를 항상 출력한다. 숨기거나 옵션으로 켜는 방식은 피한다.
- **연결 검증 우선**: 에이전트 간 연결은 입력 payload를 통해 “실제로 전달됨”이 눈에 보이도록 한다.
- **테스트 구성**: 테스트 suite는 “데모 E2E + 각 에이전트 단위 테스트”로 구성한다. 테스트 로그/아티팩트를 남겨 이유를 확인할 수 있게 한다.
- **단독 실행 우선**: 각 에이전트는 전체 시나리오 외에도 독립 실행/검증 경로가 있어야 한다.
- **루프 상한 필수**: 모든 반복 루프는 상한을 가진다. 기본값은 env로 설정하고, CLI 옵션으로 오버라이드 가능해야 한다.
- **.env 우선 로드**: 실행/테스트에서 `.env`를 기본 로드하여 API 키가 자동 반영되도록 한다.
- **새 파일은 의도된 것**: 새로 생성된 파일은 사용자 의도로 간주하고 삭제/변경 여부를 묻지 않는다.
- **프롬프트 로그 보존**: `*_req_req.md` 같은 프롬프트 로그 파일은 항상 유지한다.

## 추가 요청 사항 (앞으로의 기본 기대치)
- **리뷰 요청 시**: `docs/code-review.md` 기준으로 실제 코드 리뷰를 수행하고, 수정이 있으면 반영한다. 리뷰 결과는 `plans/` 내에 작업 중인 서브디렉토리를 찾아 `review-result.md`에 업데이트한다.
- **테스트 실행 경로 명시**: 실제 실행한 테스트 명령과 환경(`.venv/bin/python` 등)을 반드시 기록한다.
- **브랜치/상태 공유**: 응답 전에 `git status -sb` 기준으로 변경/추가 파일 상태를 확인하고, 주요 변경만 요약한다.
- **현황 문서 동기화**: README의 “조건 충족 여부/에이전트 구현 현황”은 실제 구현 상태와 항상 동기화한다.
